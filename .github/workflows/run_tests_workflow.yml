name: Run Tests
on: [push]
jobs:
  Run-Tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run
        working-directory: ./compute
        run: |
          mkdir build
          cd build
          cmake -G "Unix Makefiles" ..
          make